package mate.academy.intro.models;

public class Book {
 /*   create a Spring Boot project with pom.xml file with all required dependencies
    add Book entity
    add BookRepository
    add BookService
    configure database access
    configure checkstyle
    configure application.properties in the test/resources folder
    configure CI (Continuous Integration)
    configure GitHub git branch flow (see the Hint section below)*/
}
